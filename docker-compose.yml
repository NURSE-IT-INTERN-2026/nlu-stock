services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nlu_stock
      POSTGRES_USER: nlu_stock
      POSTGRES_PASSWORD: nlu_stock
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
