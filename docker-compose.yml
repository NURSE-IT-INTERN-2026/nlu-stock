services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: nlu_stock
      POSTGRES_USER: nlu
      POSTGRES_PASSWORD: nlu_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
