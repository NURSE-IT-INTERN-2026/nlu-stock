-- ครุภัณฑ์กับวัสดุคงทนไม่มี Lot (ADR-0002) แต่ก็รับเข้ามาเป็นงวดเหมือนกัน — ช่องนี้ให้ใบพวกนั้น
-- ตั้งชื่องวดของตัวเองได้ ว่างไว้หน้าจอจะแสดงรหัสจากวันที่รับเข้า (RCV-YYYYMMDD) แทน
ALTER TABLE "receive_records" ADD COLUMN "batchRef" TEXT;
